# Variables de entorno. .env está en .gitignore; no se sube a Git.
# Proyecto configurado SOLO para MAINNET.
#
# OBLIGATORIO rellenar estas dos líneas (ver CLAVES_PEGAR.md):
#   PRIVATE_KEY=       → clave privada de la wallet (64 hex, sin 0x). La wallet debe tener TRX en mainnet.
#   TRON_PRO_API_KEY=  → API key de https://www.trongrid.io/ (obligatoria en mainnet).
# El resto tiene valores por defecto.

PRIVATE_KEY=
TRON_PRO_API_KEY=
TOKEN_NAME=Tether USD
TOKEN_SYMBOL=USDT
TOKEN_DECIMALS=6
TOKEN_SUPPLY=1000000

# Cuenta GitHub (misma que uses en Cursor / git). Si no rellenas, el script usa git remote origin si existe.
GITHUB_USER=
GITHUB_REPO=
GITHUB_BRANCH=main
WEBSITE_URL=
