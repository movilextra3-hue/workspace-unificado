{
  "name": "bank-tokenization",
  "version": "1.0.0",
  "description": "Bank-issued bUSDT on Ethereum mainnet, 1:1 backed by USDT, SWIFT/Oracle-driven mint-redeem",
  "scripts": {
    "build": "hardhat compile",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:busdt": "hardhat run scripts/deploy-busdt.js",
    "deploy:vault": "hardhat run scripts/deploy-vault.js",
    "grant": "hardhat run scripts/grant.js",
    "reconciliation": "node reconciliation/reconciliation.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.6",
    "@openzeppelin/contracts": "^4.9.6",
    "@openzeppelin/contracts-upgradeable": "^4.9.6",
    "dotenv": "^16.4.5",
    "ethers": "^6.9.0",
    "hardhat": "^2.19.0"
  }
}