# Vitácora — 2026-03-22

## Registro del día

- **2026-03-22** — **Nuevo modelo: registro diario por archivo.** Usuario solicitó dejar de concentrar consultas y escrituras en `CONSOLIDACION_COMPLETA_TODO.md` para evitar RAM/pesadez y cierres de Cursor. Implementado: carpeta `docs/vitacora/registro/` con un archivo **`YYYY-MM-DD.md`** por día; índice `docs/vitacora/REGISTRO_DIARIO_INDICE.md`; guía `registro/README.md`. Actualizadas reglas Cursor (`vitacora-registro.mdc`, `vitacora-obligatoria-todos-proyectos.mdc`, `vitacora-al-crear-docs.mdc`), `workspace-unificado.mdc`, `00-INSTRUCCION-PRIMERA-OBLIGATORIA.mdc`, `aplicacion-total-reglas.mdc`, `interacciones-agentes.mdc` y `AGENTS.md`. En el monolito, bloque de aviso bajo «Registro de interacciones (append)»: no más append operativo ahí.
- **2026-03-22** — **Git commit local (sin push).** Usuario pidió ejecutar para que funcione guardado en repo sin subir a remoto. `git add` de reglas, `AGENTS.md`, vitácora (`REGISTRO_DIARIO_INDICE.md`, `registro/*`, aviso en monolito); `git commit` en `main`. **No** se ejecutó `git push`.
- **2026-03-22** — **Cursor: interfaz en español (usuario global).** Creado `%APPDATA%\\Cursor\\User\\argv.json` con `"locale": "es"`. Instalada extensión `MS-CEINTL.vscode-language-pack-es` v1.105.0 (`cursor --install-extension`). Añadida recomendación en `workspace-unificado/.vscode/extensions.json`. Ajuste operativo en `Cursor/User/settings.json`: `typescript.tsserver.maxTsServerMemory` 4096 y `files.maxMemoryForLargeFilesMB` 1536 (sustituyen 32768 que anulaban el workspace y favorecían OOM). Reinicio de Cursor necesario para aplicar idioma.
- **2026-03-22** — **Commit local: guardar estado para continuar al reabrir Cursor.** Incluye `.vscode/extensions.json` y actualización de este archivo en `main`; sin `git push` al remoto.
- **2026-03-22** — **Cierre de procesos opcionales “seguros” (Windows).** Usuario pidió cerrar segundo plano sin romper sistema. Ejecutado PowerShell acotado: lista corta (YourPhone, PhoneExperienceHost, Widgets, WidgetService, GameBar/XboxGameBar, etc.) + limpieza archivos en `%TEMP%` con antigüedad >7 días. Resultado comprobado: cerrados `PhoneExperienceHost` (PID 19608), `WidgetService` (PID 15228); ~1967 archivos temp antiguos eliminados. No se intentó matar procesos de sistema ni Brave/WhatsApp/Cursor; rechazado masacrar “todo lo que no sea X” por riesgo.
- **2026-03-22** — **Análisis de PID y cierre selectivo (recursos).** Revisión PowerShell: top RAM (Cursor, Java/SonarLint+Red Hat, Brave, msedgewebview2, explorer, Defender, etc.) y CPU acumulada (FxSound, Brave, audiodg, Cursor…). Cerrados por ser recuperables/no críticos: `node.exe` PID 25132 (padre inexistente, huérfano ~106 MB); todos `chrome-headless-shell` (4 instancias, ~383 MB RAM total, típico automatización/navegador embebido). No se cerraron Cursor, Brave, WhatsApp, Java de extensiones, explorer, Defender, ni FxSound (software de audio; alto CPU acumulado pero afecta experiencia de sonido).
- **2026-03-22** — **Listado completo de procesos (usuario elige cierres).** Usuario pidió lista de todos los procesos para indicar qué cerrar. Generado en sesión: 280 entradas con `PID`, nombre y `Mem_MB` (Working Set), orden alfabético por nombre de proceso.
- **2026-03-22** — **Deep Cool, CCleaner, EaseUS/Aliyun, aclaración svchost/AMD.** Usuario pidió deshabilitar Deep Cool, “deep services”, CCleaner (auto), AliyunWrapExe, y cuestionar atiesrxx/svchost. Hecho sin tocar `svchost` en bloque (riesgo sistema). Quitadas claves `HKCU\...\Run`: `DeepCool`, `CCleaner Smart Cleaning`. Script elevado `scripts/windows/disable-deepcool-ccleaner-easeus-services.ps1` ejecutado (UAC): servicios **Stopped** + **Disabled** — `Deep Cool Display Service`, `Deep Cool Helper Service`, `CCleanerPerformanceOptimizerService`, `EaseUS UPDATE SERVICE` (este último lanzaba `ensserver.exe` → `AliyunWrapExe.exe` de EaseUS). Tareas **Disabled**: `CCleanerSkipUAC - oman_`, `CCleanerSkipUAC - GBTRSD`. Documentado para usuario: `atiesrxx`/`atieclxx` = runtime AMD GPU; no deshabilitados salvo acepte riesgo en gráficos.
- **2026-03-22** — **Python/Java/Node/PowerShell “no usados”.** Usuario pidió cerrar procesos y servicios de esos runtimes no usados. Revisión: **no** hay servicios Windows relevantes (Java/Python/Node/Jupyter/Conda) con nombre coincidente; procesos activos **todos** hijos de **Cursor** (PID 2892/6024/33068): `java`×3 (Red Hat Java JRE, SonarLint JRE, Eclipse Adoptium JDK 17), `node` (helper Cursor), `python`×6 (Python 3.14 extensión), `pwsh`×6 (terminales integradas). **No** huérfanos; matarlos cerraría extensiones/LS o pestañas de terminal. Decisión: no matar en bloque; explicado al usuario que “no usado” aquí implica desactivar extensiones o cerrar terminales manualmente.
- **2026-03-22** — **Usuario: “has lo que te digo” — cierre forzado runtimes.** Ejecutado `Stop-Process -Force` sobre `java`/`javaw`, `python`/`pythonw`, `node`, y `pwsh` salvo PID de la sesión actual. Primera tanda: varios `pwsh`, 3×`java`, 6×`python`, 1×`node`. Segunda tanda tras respawn de Cursor. Tercera pasada: solo `pwsh` extra hasta quedar un único `pwsh` (sesión agente). **Nota:** Cursor **vuelve a lanzar** java/node/python mientras extensiones estén activas; para que no reaparezcan hace falta desactivar extensiones o recargar/cerrar IDE.
- **2026-03-22** — **Procesos “sospechosos”: revisión, cierre y deshabilitar arranque.** Revisión: sin ejecutables en `Temp`; rutas coherentes (Cursor, Brave, extensiones `pet.exe`/`codex.exe` legítimas). Acciones: eliminada clave `HKCU\...\Run` **dola** (Cici en `AppData\Local\Cici`); eliminada **UrbanVPN** de `HKLM\...\Run` (script elevado); servicios UrbanVPN ya **Stopped/Disabled**; detenidos **BraveUpdate**, **GooglePlayGamesServices**; detenido **FxSound** y eliminado `FxSound.lnk` de `ProgramData\...\StartUp` (script elevado). No se tocó Windows esencial, `svchost`, Defender, Brave principal, WhatsApp, Voicemod, Docker, OneDrive, Adobe, MEmu.
- **2026-03-22** — **Revisión de lo pendiente (repo + vitácora).** Trabajo local sin commit: `docs/vitacora/registro/2026-03-22.md` (entradas proceso Windows) y `scripts/windows/*.ps1` (Deep Cool/CCleaner/EaseUS, UrbanVPN HKLM, FxSound Startup). **Commit:** `3e448a8` — `chore(windows): scripts de servicios y vitácora 2026-03-22 (procesos, VPN, FxSound)`. **Sigue pendiente (sin ejecutar aquí):** `git push` a `origin/main` (rama **ahead 14**); ver `registro/2026-03-23.md` — flujo TRC20 FASE 3–4 manual OKLink / `gate:mainnet:strict` hasta verificación pública en explorador.
