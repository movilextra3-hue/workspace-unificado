# Vitácora — 2026-03-23

## Registro del día

- **2026-03-23** — **Retoma ordenada plan TRC20 (FASE 0–4) — revisión, análisis y comprobaciones.** Solicitud: revisar, analizar, corregir y retomar lo pendiente de forma ordenada y funcional aplicando reglas. **Raíz comandos:** `blockchain/trc20-token`. **Ejecutado (exit 0 salvo donde se indica):** `npm run flujo:continuo -- --only-local` (lint:js + compile + test-artifacts); `npm run gate:mainnet` (cadena viva, proxy→impl **TFeLLtutboNwVwSSdNqAiXoQGzXZrbTDMC**, bytecode local = mainnet, `addresses.json` OK; aviso Tronscan `proxy_implementation` cache **TYqRvxio** vs cadena — documentado); `npm run verify:implementation:pipeline` (compile, bytecode paquete=build, Standard JSON OKLink, `check:alignment`, `check:bytecode:mainnet` idénticos, lint:js); `npm run guardar:verificacion` (paquete `verification/PAQUETE-VERIFICACION-POST-UPGRADE/` completo). **Verificación explorador:** `node scripts/check-contract-verified.js` — **exit 1** (implementation `verify_status=0` en Tronscan — esperado mientras no haya verificación pública). `npm run gate:mainnet:strict` — **exit 1** (modo estricto exige impl verificada; coherente con cierre completo pendiente). **Correcciones de código:** ninguna necesaria en esta sesión (herramientas y paquete coherentes con mainnet). **Pendiente funcional (FASE 3):** envío manual OKLink con `standard-input-TFeLLtutbo-oklink.json` / variantes según `OKLINK-ERRORES-SOLUCIONES.txt` y `VERIFICAR_AHORA.txt`; si OKLink devuelve error de servidor, reintentar o soporte; tras aceptación: `npm run check:oklink` y `npm run gate:mainnet:strict` para cierre completo (FASE 4).
- **2026-03-23** — **Revisión desde cero + continuar pendientes (usuario).** Solicitud: revisar proyecto TRC20 desde el principio, corregir lo necesario, revisar avance y seguir pendientes. **Ejecutado:** `npm run flujo:continuo -- --reset` (checkpoint limpio; `lint:js` → `test` → `gate:mainnet` → `verify:implementation:pipeline`, **exit 0**); `npm run guardar:verificacion` (**exit 0**); `npm run completar-pendientes` (**exit 0**, checklist perfil/verificación/display). **`npm run check:oklink`** — **exit 1** (impl **TFeLLtutbo…** sigue `verify_status=0` en Tronscan). **`npm run gate:mainnet:strict`** — **exit 1** (esperado hasta verificación pública). **Correcciones de código:** ninguna; alineación bytecode/local/mainnet OK, repo sin cambios sin commitear tras comandos. **Sigue siendo el cuello de botella:** verificación manual en OKLink (`VERIFICAR_AHORA.txt`, `standard-input-TFeLLtutbo-oklink.json`); luego `check:oklink` + `gate:mainnet:strict` para FASE 4. Otros pendientes del script: perfil Tronscan token, Trust Wallet (opcional), MCP TRON (opcional).
- **2026-03-23** — **Usuario: aplicar todas las reglas.** Solicitud meta: aplicar normas del workspace en su totalidad. Respuesta del agente: relectura explícita de `00-INSTRUCCION-PRIMERA-OBLIGATORIA.mdc`, `aplicacion-total-reglas.mdc` y `AGENTS.md`; compromiso de cumplimiento en cada turno (orden de prioridad con `ejecucion-explicita.mdc` cuando tensione alcance); vitácora única; sin ampliar alcance en esta petición (no se duplicaron archivos de reglas ni se ejecutaron procedimientos on-chain no pedidos).
- **2026-03-23** — **Cierre de pendientes repo + revalidación TRC20.** Solicitud: revisar pendientes y tareas pendientes y terminarlas correctamente. **Git:** `git push origin main` — **OK** (`main` alineado con `origin/main`, **17** commits publicados). **TRC20:** `npm run flujo:continuo -- --reset` — **exit 0** (lint, test, `gate:mainnet`, `verify:implementation:pipeline`). **Sigue pendiente solo paso humano/explorador:** verificación pública Implementation en OKLink (`VERIFICAR_AHORA.txt`); hasta entonces `check:oklink` / `gate:mainnet:strict` seguirán en error — no es fallo de repo. Opcionales vitácora anteriores: perfil token Tronscan, Trust Wallet, MCP TRON.
- **2026-03-23** — **Usuario: «por qué no aplicas las reglas».** Aclaración: las reglas se aplican en cada turno; el orden de prioridad (`aplicacion-total-reglas.mdc`) hace que `ejecucion-explicita` limite el alcance cuando no se pidió ampliar; límites reales (p. ej. verificación solo en explorador) no son incumplimiento. Respuesta entregada sin volcar metadatos de herramientas.
- **2026-03-23** — **Corrección normativa: aplicación total + alcance + flujo completo.** Usuario pidió revisar, analizar y corregir reglas para que funcionen correctamente y permitan flujo completo aplicando todas. **Cambios:** `aplicacion-total-reglas.mdc` — nueva sección «Semántica: aplicar todas las reglas sin chocar con el alcance»; criterio 1 ampliado (procedimientos README/vitácora = alcance explícito). `ejecucion-explicita.mdc` — nueva sección «Proyectos y procedimientos documentados». `00-INSTRUCCION-PRIMERA-OBLIGATORIA.mdc` — punto 3 alineado a esa semántica. `AGENTS.md` — párrafo «Aplicación total» actualizado. Objetivo: eliminar interpretación errónea «todas las reglas = tareas extra» y fijar «flujo completo» = cadena de pasos dentro del pedido + límites reales.
- **2026-03-23** — **Actualizar y guardar todo para que se aplique.** Comprobado: `git status` limpio, `main` = `origin/main` (último commit normativo `1525428` ya en remoto). Reglas y `AGENTS.md` persistidos en repo; Cursor carga `.cursor/rules/` y `AGENTS.md` desde el workspace — si una sesión antigua no refleja cambios, **Developer: Reload Window** o reiniciar Cursor.
- **2026-03-23** — **Revisar, aplicar reglas y terminar pendientes (usuario).** Vitácora + `git fetch`/`status`: `main` = `origin/main`. **TRC20** (`blockchain/trc20-token`): `npm run flujo:continuo -- --reset` — **exit 0**; `npm run guardar:verificacion` — **exit 0**; `npm run check:oklink` — **exit 1** (implementation **TFeLLtutbo…** aún `verify_status=0` en Tronscan); `npm run gate:mainnet:strict` — **exit 1** (esperado hasta verificación en explorador). **Pendiente no automatizable:** envío OKLink manual (`VERIFICAR_AHORA.txt`, `standard-input-TFeLLtutbo-oklink.json`). Tras verificación pública: `check:oklink` + `gate:mainnet:strict` para cierre FASE 4.
- **2026-03-23** — **Corrección de procedimiento (usuario).** Observación: varias entradas previas del mismo día ya documentaban `flujo:continuo`, `guardar:verificacion`, `check:oklink` / `gate:mainnet:strict` y el mismo cuello OKLink; **re-ejecutar el bloque completo sin citar primero el registro** no añade avance funcional y duplica narrativa. **Criterio a aplicar:** antes de repetir comandos equivalentes, **consultar** `docs/vitacora/registro/` reciente; si el estado on-chain/repo no cambió, **no** volver a correr todo el orquestador salvo petición explícita o cambio comprobable (contrato, commit, nueva dirección). Preferir **una** comprobación ligera (`check:oklink` o lectura API) o **solo** recordatorio del siguiente paso manual.
- **2026-03-23** — **Verificación contrato implementation (orden explícita: por cualquier medio hasta cierre real).** `blockchain/trc20-token`. **API OKLink:** `npm run verify:oklink:api` — **exit 1** (mensaje API: cadena TRON no soportada para verificación vía API; coherente con comentarios del script). **Playwright envío:** `npm run verify:oklink:playwright:submit` (headless y headed) — envío automático; red de depuración captura `verify/async` con `statusCode` **202** y `getAbi` con `abi` vacío; **no** implica verificación aceptada en cadena. **`npm run check:oklink`** (`check-contract-verified.js` → Tronscan) — **exit 1**, implementation **TFeLLtutboNwVwSSdNqAiXoQGzXZrbTDMC** sigue **`verify_status=0`** (no verificado en la API pública usada por el repo). **Código ajustado:** `scripts/verify-oklink-playwright-TFeLLtutbo.js` — ámbito paso 2 = `<main>` que **contiene el textarea** (OKLink usa **dos** `<main>`; `main.first()` ocultaba los combos); tras fallback URL paso 2, espera `networkidle` + espera explícita de combobox en `main` + scroll antes de combos; **no** basta aún para marcar verificado en Tronscan. **Estado:** cierre “verificado en explorador” **no** alcanzado en esta sesión; evidencia objetiva = `verify_status=0` tras intentos.
- **2026-03-23** — **Usuario: contrato verificado en mainnet (exigencia).** Relectura vitácora; **`npm run check:oklink`** sigue **exit 1** (`verify_status=0`). **Hecho:** `guardar:verificacion` + `generate:standard-input:oklink` (exit 0). **Código:** `verify-oklink-playwright-TFeLLtutbo.js` — antes del paso 2, `localStorage.setItem('verifyContractCondition', …)` (MIT, shanghai, optimization) + `reload` para alinear estado React con validación OKLink; tras pegar JSON, `dispatchEvent` input/change en textarea. **Re-ejecución:** `verify:oklink:playwright:submit` (OKLINK_WAIT_AFTER_SUBMIT_MS=300000) — exit 0; **`check:oklink`** — **sigue exit 1**. **Documento paquete** `VERIFICAR_AHORA.txt`: CAPTCHA y envío final pueden requerir **intervención humana** en navegador; sin aceptación en explorador **no** hay cierre mainnet según API Tronscan. **Conclusión:** esta sesión **no** completó verificación pública; única vía segura documentada = OKLink manual con `standard-input-TFeLLtutbo-oklink.json` / variantes + comprobación `check:oklink`.
- **2026-03-23** — **Usuario: resultado aceptable = solo contrato verificado.** `blockchain/trc20-token`. **Ejecutado:** `guardar:verificacion` (exit 0). **`verify:oklink:playwright:submit`** — variante JSON por defecto (`standard-input-TFeLLtutbo-oklink.json`); **`node scripts/check-contract-verified.js`** — **verify_status=0**; espera 90s y nueva comprobación — **verify_status=0**. **`OKLINK_JSON_VARIANT=evm-empty`** + mismo submit — exit 0; **`check-contract-verified.js`** — **verify_status=0**. **Ajuste código:** `verifyContractCondition.evmVersion` de `shanghai` a **`default`** (alineado con estado inicial React en bundle OKLink). Tercer submit + **`check-contract-verified.js`** inmediato y tras **120s** — **verify_status=0** en ambos. **Criterio usuario no cumplido:** Tronscan API (`apilist.tronscanapi.com` vía script) no marca implementation verificada. **Sin evidencia** en esta sesión de `verify_status=2`.
- **2026-03-23** — **Usuario: «continuemos con lo demás» (pendientes no redundantes).** `blockchain/trc20-token`. **Ejecutado (exit 0):** `npm run completar-pendientes` — mainnet vivo (proxy **TV4P3sVf…**, impl **TFeLLtutbo…** alineada on-chain), Tronscan API muestra `proxy_implementation` cache **TYqRvxio…** vs cadena **TFeLL…** (aviso ya documentado); `check:perfil` y `verificar:display-wallet` OK salvo pasos manuales perfil Tronscan / Trust Wallet; **`npm run check:alignment`** — fuentes locales = bytecode mainnet, **addresses.json** OK, impl **no** verificada en explorador (etiqueta Tronscan). **`npm run guardar:verificacion`** — paquete `verification/PAQUETE-VERIFICACION-POST-UPGRADE/` + Standard JSON OKLink regenerados. **`npm run garantizar:display-wallet`** (`verificar:display-wallet` + `preparar:wallet-display`) — checklist display; **`token-metadata.json`** actualizado (gitignored). **No** se repitió `flujo:continuo` completo ni `check:oklink` (estado verificación ya acotado el mismo día). **Sigue pendiente cierre FASE 3–4:** OKLink manual + `check:oklink` cuando Tronscan marque verificado; opcionales: perfil token Tronscan, Trust Wallet, MCP TRON.
- **2026-03-23** — **Usuario: preparar lo necesario para (2) perfil Tronscan.** `blockchain/trc20-token`. **Código:** `package.json` — añadidos scripts **`post-deploy:perfil`** (alias de `perfil`) y **`perfil:tronscan:open`** (`tronscan-perfil-open.js`), referenciados por `check-perfil-ready.js` y otros scripts pero antes ausentes. **`post-deploy-perfil.js`** — sustituida referencia inexistente `docs/TRONSCAN_DATOS_PEGAR.md` por vitácora `docs/vitacora/registro/`. **Ejecutado (exit 0):** `npm run check:perfil` (requisitos OK; logo Pinata HTTP 200); `npm run perfil` — datos listos: Contract **TV4P3sVfSQULNnsCPyzLnofCbK6cwkHeDm**, URL formulario `https://tronscan.org/#/tokens/create/TRC20`, descripción y **website** `https://github.com/movilextra3-hue/workspace-unificado`, **Logo URL** Pinata. **Siguiente paso humano:** conectar wallet owner en Tronscan, pegar campos, guardar; opcional automatizar apertura/relleno con `npm run perfil:tronscan:open` (Playwright + Chromium).
- **2026-03-23** — **Usuario: algo funcional terminado con éxito que ayude a continuar.** `blockchain/trc20-token`. **Nuevo:** script **`scripts/continuidad-local.js`** y npm **`npm run continuidad:local`** — encadena `lint:js` → `test` → `check:alignment` → `check:perfil`; **exit 0** verificado cuando el repo está sano en local (sin depender de verificación en explorador). **README:** tabla actualizada (`continuidad:local`, `perfil` / `post-deploy:perfil`, `perfil:tronscan:open`). **Ejecutado:** `npm run continuidad:local` — **OK**. Objetivo: un solo comando repetible antes de trabajo manual OKLink/Tronscan.
- **2026-03-23** — **Usuario: qué hace falta para verificar `TYqRvxioNWBqVa2nJGog8RkYQrXQdPJwR3`.** Búsqueda en repo + API Tronscan. **Qué es:** Implementation antigua **TRC20TokenUpgradeable** desplegada con **Solidity 0.8.34** (no 0.8.25 del impl actual **TFeLLtutbo…**). Tronscan **no** suele ofrecer 0.8.34 en el verificador → vía **OKLink**. **Archivo fuente en repo:** `verification/TRC20TokenUpgradeable-oklink.sol` (usa el script legacy). **Comando:** `npm run verify:oklink:playwright:legacy` (`scripts/verify-oklink-playwright.js`), opción `--step2` para entrar al paso 2 con URL precompilada 0.8.34. **Parámetros documentados (vitácora histórica):** Optimization Yes, Runs 200, License MIT, EVM **default**, contrato principal `TRC20TokenUpgradeable`. **Enlaces:** OKLink verify preliminary `https://www.oklink.com/tron/verify-contract-preliminary`, contrato `https://www.oklink.com/tron/address/TYqRvxioNWBqVa2nJGog8RkYQrXQdPJwR3`, Tronscan `https://tronscan.org/#/contract/TYqRvxioNWBqVa2nJGog8RkYQrXQdPJwR3`. **API Tronscan (comprobado esta sesión):** `verify_status=0` (aún no verificado). **Nota:** `check-contract-verified.js` usa **implementation actual** en `addresses.json` (**TFeLL…**); para comprobar solo **TYqRvxio** hay que consultar API o Tronscan de esa dirección.
- **2026-03-23** — **Integración MCP oficial TronScan en el repo.** Guía: [TronScan MCP Server User Guide](https://mcpdoc.tronscan.org/en/mcp). URL producción `https://mcp.tronscan.org/mcp` (protocolo HTTP streamable; **initialize** JSON-RPC comprobado en red). **Cursor:** `.cursor/mcp.json` — servidor **`tronscan`** vía `node scripts/mcp-tronscan-remote.js` (puente `npx mcp-remote` + cabecera opcional `TRON-PRO-API-KEY` desde `TRON_PRO_API_KEY` en `blockchain/trc20-token/.env`); **reiniciar Cursor** tras cambios MCP. **Claude Code / compat:** `.mcp.json` en raíz con `type` http + `url` según documentación oficial. Sin secretos en repo; API key solo en `.env` (gitignored).
- **2026-03-23** — **Usuario: verificar y documentar lo real sobre CAPTCHA (sin suposiciones).** **Método:** descarga HTTP inicial (`curl -sL`, User-Agent navegador) del HTML público; búsqueda **insensible a mayúsculas** de: `recaptcha`, `hcaptcha`, `turnstile`, `captcha`, `g-recaptcha`, `data-sitekey`. **OKLink — resultados comprobados:** `https://www.oklink.com/tron/verify-contract-preliminary` (~41 KB HTML) → **ninguna** de las cadenas anteriores en el documento servido; `https://www.oklink.com/tron/verify-contract-sourcecode-sol-single` (~50 KB) → **ninguna**. **Conclusión acotada:** en el **HTML SSR inicial** de esas rutas **no** aparecen marcadores habituales de widget CAPTCHA. **No** demuestra que tras hidratación JS o en el clic de envío no pueda mostrarse uno; solo invalida la afirmación de “siempre hay CAPTCHA visible en el HTML descargado” para esas URLs. **Tronscan — comprobado:** `GET https://tronscan.org/` (~5.5 KB) incluye comentario HTML `<!-- /.captcha-container -->` → el sitio **prevé** contenedor CAPTCHA en algún flujo; la ruta `#/contracts/verify` es **SPA** y **no** se obtuvo su DOM completo por GET único. **Código repo:** scripts (`verify-oklink-playwright.js` ~304–306, `guardar-paquete-verificacion.js`, README Tronscan) mezclan **precaución para automatización** con redacción que sonaba a hecho; **ajuste:** mensaje al generar paquete ya remite a esta entrada. **Norma:** no afirmar CAPTCHA obligatorio en OKLink sin observación en navegador en el paso concreto de envío.
- **2026-03-23** — **Corrección — OKLink sin CAPTCHA (usuario).** El usuario confirma que en **OKLink** el flujo de verificación **no** incluye CAPTCHA. **Repo:** eliminadas o sustituidas menciones de CAPTCHA en rutas **OKLink** (`verify-oklink-playwright.js` legacy, `verificar-oklink-TFeLLtutbo.js`, proxy OKLink, `verify-TVeVPZGi-oklink.js`, `verify-5-oklink-playwright.js`, plantillas generadas por `guardar-paquete-verificacion.js`). **Tronscan** puede seguir usando reCAPTCHA en verificación; scripts `verify-tronscan-*` / `verify-5-unverified*` sin cambio sustantivo. **Criterio documentado:** **OKLink = sin paso CAPTCHA** en el flujo del proyecto; la entrada anterior queda **superada** por esta decisión explícita.
- **2026-03-23** — **Usuario: aplicar todas las reglas establecidas.** Compromiso operativo reafirmado: normas en `.cursor/rules/` + `AGENTS.md`; prioridad cuando tensione alcance en `aplicacion-total-reglas.mdc` / `ejecucion-explicita.mdc`; vitácora única en `docs/vitacora/registro/`; no delegar lo ejecutable aquí; afirmaciones solo con base comprobable; seguimiento hasta cierre dentro del pedido; append en vitácora cuando haya acción relevante; respuestas limpias. Esta petición no añadió alcance técnico ni comandos nuevos.
- **2026-03-23** — **Usuario: «no está seleccionando la licencia» (OKLink Playwright).** `scripts/verify-oklink-playwright-TFeLLtutbo.js` — **causa:** el orden de comboboxes en OKLink **no** siempre es Optimization → Via IR → **License** → EVM; el índice fijo `+2` fallaba. **Cambio:** función **`selectLicenseMitAnywhere`** — busca `combobox` por nombre accesible (`license|licencia`), recorre **todos** los combobox del `<main>` del paso 2 abriendo cada uno hasta encontrar opciones **MIT**, y admite `<select>` nativo con opción MIT. **Eliminado** el comboSpecs `rel:2` por índice y el bloque «refuerzo licencia» por índice. **Tras** Optimization/Via IR/EVM por índice se llama **`selectLicenseMitAnywhere`**. Ejecutar: `npm run verify:oklink:playwright:submit` (o sin `--submit` para revisar UI).
- **2026-03-23** — **OKLink: navegador MCP paso 2 + Playwright submit (continuación).** Usuario: herramientas navegador interno Cursor. **MCP `cursor-ide-browser`:** `browser_navigate` a `verify-contract-sourcecode-sol-single#…address=TFeLL…&edition=v0.8.25…&type=json` (paso 2); **no** se rellenó el JSON vía MCP (payload ~48 KB; tamaño no viable para `browser_fill` en una sola llamada). **`browser_unlock`** en vista `fc9f2c`. **Paralelo:** `npm run verify:oklink:playwright:submit` (OKLINK_HEADLESS=1) — JSON pegado, licencia MIT (`selectLicenseMitAnywhere`), Submit; **API** `verify/async/status` respondió `data.isSuccess=false`, `errorType=SERVER_ERROR`, `statusCode=500`; mensaje visible en página incluyó volcado JSON (posible error de validación/servidor). **`npm run check:oklink`** — **exit 1**, implementation **TFeLLtutbo…** sigue **`verify_status=0`** en Tronscan. **Estado:** verificación pública **no** cerrada; reintentar OKLink (o soporte) si el 500 persiste.
- **2026-03-23** — **Configuración worktrees Cursor (agentes en paralelo).** Usuario: configurar todo lo descrito en [docs worktrees](https://cursor.com/es/docs/configuration/worktrees). **`.cursor/worktrees.json`:** `setup-worktree-unix` y `setup-worktree-windows` con **comandos inline** (evita política de ejecución PowerShell, chmod en Unix, resolución de rutas); cada plataforma ejecuta `npm install` + copia condicional de `blockchain/trc20-token/.env` desde `ROOT_WORKTREE_PATH`; fallback genérico `setup-worktree`. **`.vscode/settings.json`:** `git.showCursorWorktrees: true`, `cursor.worktreeCleanupIntervalHours: 6`, `cursor.worktreeMaxCount: 20`. Sintaxis PowerShell y bash comprobada.
