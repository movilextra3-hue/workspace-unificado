# Configuración Ruff para lint de Python (apps RTSP)
[tool.ruff]
target-version = "py311"
line-length = 120
exclude = ["node_modules", "venv", "__pycache__", ".git"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
